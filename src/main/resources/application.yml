server:
  port: 9333

spring:
  profiles:
    active: dev
  application:
    name: 通用后台内容管理系统

# mybatis对应的映射文件路径
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.julibo.pig.cms.model

